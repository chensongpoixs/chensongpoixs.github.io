# Jekyll 博客本地运行指南

## 📋 第一步：安装 Ruby（Windows）

### 方法一：使用 RubyInstaller（推荐）

1. **下载 RubyInstaller**
   - 访问：https://rubyinstaller.org/downloads/
   - 下载 **Ruby+Devkit 3.1.x** 或更高版本（推荐 3.1.5 或 3.2.x）
   - 选择 `.exe` 安装程序

2. **安装 Ruby**
   - 运行下载的安装程序
   - ✅ **重要**：勾选 "Add Ruby executables to your PATH"
   - ✅ **重要**：勾选 "Associate .rb and .rbw files with this Ruby installation"
   - 点击 "Install" 完成安装

3. **验证安装**
   - 打开新的 PowerShell 或 CMD 窗口
   - 运行：`ruby -v`
   - 应该显示 Ruby 版本号

### 方法二：使用 Chocolatey（如果已安装）

```powershell
choco install ruby
```

## 📦 第二步：安装 Jekyll 和依赖

打开 PowerShell 或 CMD，在项目目录下运行：

```bash
# 安装 Bundler
gem install bundler

# 安装项目依赖
bundle install
```

如果遇到权限问题，可能需要以管理员身份运行。

## 🚀 第三步：启动本地服务器

在项目目录下运行：

```bash
# 方法一：使用 Bundler（推荐）
bundle exec jekyll serve

# 方法二：直接使用 Jekyll
jekyll serve
```

## 🌐 第四步：访问博客

打开浏览器访问：
- **本地访问**: http://localhost:4000
- **局域网访问**: http://你的IP:4000

## ⚙️ 常用命令

```bash
# 启动服务器（监听文件变化）
jekyll serve --watch

# 启动服务器（允许局域网访问）
jekyll serve --host 0.0.0.0

# 使用自定义端口
jekyll serve --port 4001

# 构建静态网站
jekyll build

# 清理生成的文件
jekyll clean
```

## 🔧 故障排除

### 问题1: `gem` 命令未找到
**原因**: Ruby 未安装或未添加到 PATH
**解决**: 
1. 重新安装 Ruby，确保勾选 "Add Ruby executables to your PATH"
2. 重启 PowerShell/CMD
3. 检查 PATH 环境变量

### 问题2: 安装 gem 时权限错误
**解决**: 以管理员身份运行 PowerShell

### 问题3: `bundle install` 失败
**解决**: 
```bash
# 更新 bundler
gem update bundler

# 清理并重新安装
bundle clean --force
bundle install
```

### 问题4: Windows 上 wdm 错误
**解决**: 已在 Gemfile 中包含，运行 `bundle install` 即可

### 问题5: 端口 4000 被占用
**解决**: 使用其他端口
```bash
jekyll serve --port 4001
```

## 📝 开发提示

- 修改文件后，Jekyll 会自动重新生成（使用 `--watch` 参数）
- 修改 `_config.yml` 后需要重启服务器
- 生成的文件在 `_site` 目录（不要手动编辑）

## 🎯 快速启动（安装完成后）

1. 打开 PowerShell
2. 进入项目目录：`cd D:\Work\chensongpoixs.github.io`
3. 运行：`bundle exec jekyll serve`
4. 打开浏览器访问：http://localhost:4000

---

**需要帮助？** 查看 Jekyll 官方文档：https://jekyllrb.com/docs/

