# 🚀 Jekyll 博客快速启动指南

## ⚠️ 当前状态
系统检测到 **Ruby 未安装**，需要先安装 Ruby 才能运行项目。

---

## 📥 第一步：安装 Ruby（必须）

### Windows 安装步骤：

1. **下载 RubyInstaller**
   - 访问：https://rubyinstaller.org/downloads/
   - 下载 **Ruby+Devkit 3.1.x** 或 **3.2.x** 版本
   - 推荐下载：`rubyinstaller-devkit-3.1.5-x64.exe` 或更高版本

2. **安装 Ruby**
   - 运行下载的 `.exe` 安装程序
   - ✅ **重要**：勾选 "Add Ruby executables to your PATH"
   - ✅ **重要**：勾选 "Associate .rb and .rbw files with this Ruby installation"
   - 点击 "Install" 完成安装
   - 安装完成后会弹出 MSYS2 安装窗口，选择 "3" 完成安装

3. **验证安装**
   - **关闭当前 PowerShell/CMD 窗口**
   - **重新打开** PowerShell 或 CMD
   - 运行：`ruby -v`
   - 应该显示 Ruby 版本号（如：ruby 3.1.5）

---

## 📦 第二步：安装依赖

打开 **新的** PowerShell 窗口，进入项目目录：

```powershell
cd D:\Work\chensongpoixs.github.io
```

然后运行：

```powershell
# 安装 Bundler
gem install bundler

# 安装项目依赖
bundle install
```

如果遇到权限问题，请以**管理员身份**运行 PowerShell。

---

## 🎯 第三步：启动服务器

### 方法一：使用批处理脚本（推荐）

直接双击运行：`启动服务器.bat`

### 方法二：手动启动

在项目目录下运行：

```powershell
bundle exec jekyll serve
```

或者（带自动刷新）：

```powershell
bundle exec jekyll serve --watch
```

---

## 🌐 第四步：访问博客

打开浏览器访问：
- **本地访问**: http://localhost:4000
- **局域网访问**: http://你的IP:4000

---

## ⚡ 快速命令参考

```powershell
# 启动服务器（基础）
bundle exec jekyll serve

# 启动服务器（监听文件变化）
bundle exec jekyll serve --watch

# 启动服务器（允许局域网访问）
bundle exec jekyll serve --host 0.0.0.0

# 使用自定义端口
bundle exec jekyll serve --port 4001

# 构建静态网站
bundle exec jekyll build

# 清理生成的文件
bundle exec jekyll clean
```

---

## 🔧 常见问题

### 问题1: `ruby` 命令未找到
**原因**: Ruby 未安装或未添加到 PATH  
**解决**: 
1. 重新安装 Ruby，确保勾选 "Add Ruby executables to your PATH"
2. **重启 PowerShell/CMD 窗口**
3. 检查环境变量 PATH 中是否包含 Ruby 路径

### 问题2: `gem install` 权限错误
**解决**: 以管理员身份运行 PowerShell

### 问题3: `bundle install` 失败
**解决**: 
```powershell
# 更新 bundler
gem update bundler

# 清理并重新安装
bundle clean --force
bundle install
```

### 问题4: 端口 4000 被占用
**解决**: 使用其他端口
```powershell
bundle exec jekyll serve --port 4001
```

---

## 📝 安装完成后的标准流程

1. ✅ 安装 Ruby（重启命令行）
2. ✅ 运行 `gem install bundler`
3. ✅ 运行 `bundle install`
4. ✅ 运行 `bundle exec jekyll serve`
5. ✅ 访问 http://localhost:4000

---

**需要帮助？** 查看详细文档：`安装和运行指南.md`

